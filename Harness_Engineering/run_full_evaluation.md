Run the full evaluation. Print one line per case as it completes: case number, agent,
tool calls attributed. When everything finishes, print a final line:
"EVAL COMPLETE — attribution check PASSED/FAILED — results written: yes/no".

Then, for every case v2 failed, rerun that case alone three times and report each
outcome with the full transcript.
