The mock API is running and exposed at <PASTE TUNNEL URL>.

Add a servers block with that URL to both specs/transfer-v1.yaml and
specs/transfer-v2.yaml, identically. Change nothing else. Show me the diff.
