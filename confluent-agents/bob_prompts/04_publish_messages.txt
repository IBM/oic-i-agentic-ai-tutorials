Update produce_messages.py and sample-transactions.json for this Flink tutorial. Load only the existing .env variables: CONFLUENT_CLOUD_API_KEY, CONFLUENT_CLOUD_API_SECRET, CONFLUENT_ENVIRONMENT_ID, CONFLUENT_CLUSTER_ID, CONFLUENT_CLUSTER_REST_ENDPOINT, TOPIC_NAME, TOPIC_PARTITIONS, TOPIC_RETENTION_MS. Do not require BOOTSTRAP_SERVERS, KAFKA_API_KEY, KAFKA_API_SECRET, or KSQLDB_* variables. Publish 20 JSON records to TOPIC_NAME using the Confluent Cloud Kafka REST Produce endpoint. Each record must contain sku, branch, quantity, and transaction_type. quantity must always be non-negative; transaction_type must be ADDITION or SALE. Use a stable key derived from sku and branch when possible. Build the sample data so LAPTOP-DELL-XPS-15 in MallOfEgypt ends at exactly 0, and LAPTOP-MACBOOK-PRO-16 in MallOfEgypt remains above 0. Check the error_code in every REST delivery report even when HTTP status is 200. Never print the API secret. After publishing, validate that Flink updates inventory.availability and report the expected final quantities for at least two SKU/branch pairs. Produce messages as raw JSON using "type": "JSON" in the REST payload. Do not use Avro, fastavro, or Schema Registry — the Flink source table auto-infers raw JSON topics and no schema registration is needed.