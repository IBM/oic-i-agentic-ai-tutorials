In the confluent-agents project, create or update create_topic.py to create the Kafka topic using the Confluent Cloud Kafka REST API. Load configuration only from .env with python-dotenv. Use exactly these variables: CONFLUENT_CLOUD_API_KEY, CONFLUENT_CLOUD_API_SECRET, CONFLUENT_ENVIRONMENT_ID, CONFLUENT_CLUSTER_ID, CONFLUENT_CLUSTER_REST_ENDPOINT, TOPIC_NAME, TOPIC_PARTITIONS, TOPIC_RETENTION_MS. Do not introduce BOOTSTRAP_SERVERS, KAFKA_API_KEY, KAFKA_API_SECRET, or KSQLDB_* aliases. Use HTTP Basic authentication with the Kafka cluster API key and secret. Validate all required variables, but never print the API secret. Make the script idempotent: if the topic already exists, validate it instead of failing. Create the topic with TOPIC_PARTITIONS and retention.ms=TOPIC_RETENTION_MS, then GET the topic and its retention.ms configuration and print a concise validation summary. Also update .env.example with placeholder values using the exact structure above, update requirements.txt, and ensure .env is ignored by Git.