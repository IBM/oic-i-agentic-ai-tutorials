spec_version: v1
kind: connection
app_id: ariba_approvals_key_value
environments:
    draft:
        kind: key_value
        type: team
        sso: false
        credentials:
          client_id: "{ARIBA[APPROVALS][client_id]}"
          client_secret: "{ARIBA[APPROVALS][client_secret]}"
          api_key: "{ARIBA[APPROVALS][api_key]}"
    live:
        kind: key_value
        type: team
        sso: false
        credentials:
          client_id: "{ARIBA[APPROVALS][client_id]}"
          client_secret: "{ARIBA[APPROVALS][client_secret]}"
          api_key: "{ARIBA[APPROVALS][api_key]}"