spec_version: v1
kind: connection
app_id: ibm_cos_key_value
environments:
  draft:
    kind: key_value
    type: team
    sso: false
    server_url: "{IBM_COS[base_url]}"
    credentials:
      api_key: "{IBM_COS[api_key]}"
      instance_id: "{IBM_COS[instance_id]}"
      base_url: "{IBM_COS[base_url]}"
  live:
    kind: key_value
    type: team
    sso: false
    server_url: "{IBM_COS[base_url]}"
    credentials:
      api_key: "{IBM_COS[api_key]}"
      instance_id: "{IBM_COS[instance_id]}"
      base_url: "{IBM_COS[base_url]}"
