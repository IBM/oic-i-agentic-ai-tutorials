spec_version: v1
kind: connection
app_id: jenkins_basic
environments:
    draft:
        kind: basic
        type: team
        sso: false
        server_url: "{JENKINS[base_url]}"
        credentials:
            username: "{JENKINS[username]}"
            password: "{JENKINS[password]}"
    live:
        kind: basic
        type: team
        sso: false
        server_url: "{JENKINS[base_url]}"
        credentials:
            username: "{JENKINS[username]}"
            password: "{JENKINS[password]}"
