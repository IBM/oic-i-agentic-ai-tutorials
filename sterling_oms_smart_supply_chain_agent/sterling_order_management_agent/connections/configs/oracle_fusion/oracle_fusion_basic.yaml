spec_version: v1
kind: connection
app_id: oracle_fusion_basic
environments:
    draft:
        kind: basic
        type: team
        sso: false
        server_url: "{ORACLE_FUSION[base_url]}"
        credentials:
            username: "{ORACLE_FUSION[username]}"
            password: "{ORACLE_FUSION[password]}"
    live:
        kind: basic
        type: team
        sso: false
        server_url: "{ORACLE_FUSION[base_url]}"
        credentials:
            username: "{ORACLE_FUSION[username]}"
            password: "{ORACLE_FUSION[password]}"