# ── Core agent deps ───────────────────────────────────────────────────────────
langgraph>=1.2.11
langchain>=1.4.0
langchain-core>=1.6.2
langchain-google-genai>=4.4.0
python-dotenv>=1.0.0

# ── A2A server deps ───────────────────────────────────────────────────────────
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
pydantic>=2.13.5
httpx>=0.27.0

# ── wxO observability (optional — comment out if not using telemetry) ─────────
opentelemetry-api>=1.44.0
opentelemetry-sdk>=1.44.0
opentelemetry-exporter-otlp-proto-http>=1.44.0

# ── watsonx Orchestrate SDK (optional — for decorator-based tracing) ──────────
ibm-watsonx-orchestrate>=2.16.1
