spec_version: v1
kind: connection
app_id: "<APP_ID_NAME>"
environments:
    draft:
        kind: basic
        type: team
        sso: false
        server_url: "{base_url}"
        credentials:
            username: "{username}"
            password: "{password}"
    live:
        kind: basic
        type: team
        sso: false
        server_url: "{base_url}"
        credentials:
            username: "{username}"
            password: "{password}"