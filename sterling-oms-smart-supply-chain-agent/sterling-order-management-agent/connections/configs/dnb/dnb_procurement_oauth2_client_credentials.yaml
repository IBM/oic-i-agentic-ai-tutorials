spec_version: v1
kind: connection
app_id: dnb_procurement_oauth2_client_credentials
environments:
    draft:
        kind: oauth_auth_client_credentials_flow
        type: member
        sso: false
        server_url: "{DNB[base_url]}"
        credentials:
            token_url: "{DNB[token_url]}"
            client_id: "{DNB[PROCUREMENT][client_id]}"
            client_secret: "{DNB[PROCUREMENT][client_secret]}"
            scope: "{DNB[PROCUREMENT][scope]}"
    live:
        kind: oauth_auth_client_credentials_flow
        type: member
        sso: false
        server_url: "{DNB[base_url]}"
        credentials:
            token_url: "{DNB[token_url]}"
            client_id: "{DNB[PROCUREMENT][client_id]}"
            client_secret: "{DNB[PROCUREMENT][client_secret]}"
            scope: "{DNB[PROCUREMENT][scope]}"
